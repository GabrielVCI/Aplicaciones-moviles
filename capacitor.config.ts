import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App with React',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
